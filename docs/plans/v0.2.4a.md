v0.2.4a: The Loadout (Data Integration)

Here is the comprehensive implementation plan for v0.2.4a: The Loadout (Data Integration).

This sub-version focuses on the data infrastructure required to give enemies unique movesets. It connects the EnemyTemplate blueprints (defined in v0.2.2a) with the ActiveAbility system (defined in v0.2.3b), ensuring that when a "Rusted Draugr" spawns, it knows how to use "Rusty Cleave."

Goal: Establish the Many-to-Many relationship between Enemies and Abilities, seed enemy-specific skills, and update the instantiation pipeline to hydrate enemies with their combat loadouts.

1. Architecture & Data Flow
The Hydration Pipeline
We must bridge the gap between the static EnemyTemplate (blueprint) and the dynamic Combatant (runtime entity).
	1	Definition (Seeding): EnemyTemplate now includes a list of AbilityNames (strings).
	2	Resolution (Factory): When EnemyFactory.CreateFromTemplate() is called:
	◦	The factory injects IActiveAbilityRepository.
	◦	It queries the repository for abilities matching the template's list.
	◦	It populates the Enemy.Abilities collection.
	3	Adaptation (Combat): When Combatant.FromEnemy() is called:
	◦	It copies Enemy.Abilities into Combatant.Abilities.
	◦	The CombatService can now access these abilities for AI decision-making.
Data Schema
	•	ActiveAbilities Table: Already exists (v0.2.3c).
	•	EnemyAbilities Join Table (Implicit): We will manage this relationship in the Enemy entity logic rather than a strict DB join table for now, as Enemy entities are often transient or serialized as part of GameState.

2. Logic Decision Trees
A. Enemy Creation Logic (Factory Level)
Input: TemplateID ("und_draugr_01"), PartyLevel
	1	Fetch Template: Retrieve EnemyTemplate record.
	2	Create Base Enemy: Instantiate Enemy object with scaled stats (v0.2.2a logic).
	3	Check Ability Definition: Does Template have AbilityNames?
	◦	No: Assign default "Basic Attack" ability.
	◦	Yes: Iterate through names. 4.  Repository Lookup: Does ability exist in DB? *  Yes: Add to Enemy.Abilities. *  No: Log Warning ("Template references missing ability"), skip.
	4	Return: Fully hydrated Enemy.
B. Fallback Logic
Input: Enemy with empty Ability list
	1	Validation: Combatant.FromEnemy checks ability count.
	2	Correction: If count == 0:
	◦	Inject generic "Strike" ability (Cost: 0, Damage: WeaponDice).
	◦	Log Warning ("Enemy spawned without abilities").

3. Code Implementation
A. Core Layer (Models)
File: RuneAndRust.Core/Models/Combat/EnemyTemplate.cs
public record EnemyTemplate(
    string Id,
    string Name,
    // ... existing properties ...
    List<string> AbilityNames // NEW: List of ability names to look up
);
File: RuneAndRust.Core/Entities/Enemy.cs
public class Enemy
{
    // ... existing properties ...

    // NEW: The abilities this specific enemy instance can use
    public List<ActiveAbility> Abilities { get; set; } = new();
}
B. Engine Layer (Factory)
File: RuneAndRust.Engine/Factories/EnemyFactory.cs
public class EnemyFactory : IEnemyFactory
{
    private readonly IActiveAbilityRepository _abilityRepo;
    // ... other dependencies ...

    public async Task<Enemy> CreateFromTemplateAsync(EnemyTemplate template, int partyLevel)
    {
        var enemy = new Enemy
        {
            Name = template.Name,
            // ... apply scaling logic ...
        };

        // Hydrate Abilities
        if (template.AbilityNames != null && template.AbilityNames.Any())
        {
            foreach (var abilityName in template.AbilityNames)
            {
                var ability = await _abilityRepo.GetByNameAsync(abilityName);
                if (ability != null)
                {
                    enemy.Abilities.Add(ability);
                }
                else
                {
                    _logger.LogWarning("Missing ability '{Ability}' for enemy '{Enemy}'", abilityName, template.Id);
                }
            }
        }

        // Ensure at least one ability exists (Fallback)
        if (!enemy.Abilities.Any())
        {
             // Logic to add a default "Attack" ability
        }

        return enemy;
    }
}
C. Data Layer (Seeder)
```csharp
File: RuneAndRust.Persistence/Data/AbilitySeeder.cs
// Adding Enemy-specific abilities
new ActiveAbility
{
    Name = "Rusty Cleave",
    Archetype = ArchetypeType.Warrior, // Or a new 'Enemy' type if strict separation desired
    Tier = 1,
    Cost = 15,
    ResourceType = ResourceType.Stamina,
    EffectScript = "DAMAGE:Physical:1d8+2;STATUS:Tetanus:3" // Tetanus = Poison/Disease variant
},
new ActiveAbility
{
    Name = "Sonic Screech",
    Archetype = ArchetypeType.Mystic,
    Tier = 2,
    Cost = 20,
    ResourceType = ResourceType.Stamina, // Enemies use Stamina for "mana" usually
    EffectScript = "DAMAGE:Psychic:1d6;STATUS:Disoriented:2"
}
```

4. Logging Requirements
Reference: SPEC-CORE-LOGGING
System
Event
Level
Message Template
Properties
Factory
Hydration Start
Debug
"Hydrating abilities for {Enemy}: {Count} templates found."
Enemy, Count
Factory
Ability Added
Trace
"Added ability {Ability} to {Enemy}."
Ability, Enemy
Factory
Missing Ability
Warning
"Template {Template} references missing ability {Ability}."
Template, Ability
Combat
Adapter Sync
Trace
"Synced {Count} abilities from Enemy to Combatant."
Count

5. Testing Requirements
Unit Tests (EnemyFactoryTests.cs)
	•	Hydration_LoadsAbilities:
	    ◦	Mock IActiveAbilityRepository to return "Rusty Cleave".
	    ◦	Create Template with AbilityNames = ["Rusty Cleave"].
	    ◦	Call CreateFromTemplateAsync.
	    ◦	Assert result.Abilities contains "Rusty Cleave".
	•	Hydration_SkipsMissing:
	    ◦	Template requests "Mega Death Laser".
	    ◦	Repository returns null.
	    ◦	Assert result.Abilities does not contain nulls.
	•	Fallback_EnsureBasicAttack:
	    ◦	Template has empty abilities.
	    ◦	Assert result.Abilities contains default "Attack".
Integration Tests (EnemyPersistenceTests.cs)
	•	Seeding_VerifyEnemyAbilities:
	◦	Run Seeder.
	◦	Query ActiveAbilities.
	◦	Assert "Rusty Cleave" exists in database.

6. Deliverable Checklist (v0.2.4a)
	•	Core:
	◦	[ ] Update EnemyTemplate record with AbilityNames.
	◦	[ ] Update Enemy entity with List<ActiveAbility>.
	◦	[ ] Update Combatant.FromEnemy to copy abilities.
	•	Engine:
	◦	[ ] Update EnemyFactory to inject IActiveAbilityRepository.
	◦	[ ] Implement async hydration logic in CreateFromTemplateAsync.
	•	Data:
	◦	[ ] Update AbilitySeeder with 5-10 enemy abilities.
	◦	[ ] Update EnemySeeder (Templates) to reference these new abilities.
	•	Tests:
	◦	[ ] Update existing Factory tests to mock the new repo dependency.
	◦	[ ] Add hydration unit tests.

7. Draft Changelog (v0.2.4a)
```markdown
# Changelog: v0.2.4a - The Loadout (Data Integration)
**Release Date:** 2025-XX-XX

## Summary
The "Loadout" update lays the groundwork for complex enemy behavior. It establishes the data pipeline allowing enemies to possess and use the same Active Abilities as players. The Enemy Factory now "hydrates" spawned enemies with specific move sets defined in their templates (e.g., Draugr now spawn knowing "Rusty Cleave").

## Features
- **Enemy Ability Hydration:**
    - Enemies are no longer limited to basic attacks.
    - `EnemyFactory` now queries the Ability Database to populate enemy loadouts upon spawn.
- **New Enemy Abilities:**
    - Seeded new skills specifically for enemies, including *Rusty Cleave* (DoT), *Sonic Screech* (Debuff), and *Shield Bash* (Stun).

## Technical
- Extended `EnemyTemplate` to support string-based ability lookups.
- Refactored `EnemyFactory` to support asynchronous ability fetching.
- Updated `Combatant` adapter to carry enemy abilities into the Combat State.
```