Here is the comprehensive implementation plan for v0.2.2: The Adversary, split into manageable sub-versions. This milestone transforms enemies from static punching bags into distinct threats with unique behaviors, stats, and loot tables.

Milestone 3, Phase 3: The Adversary (v0.2.2)
v0.2.2a: The Bestiary (Data & definitions)
Goal: Establish the data structures for distinct enemy types, integrating SPEC-COMBAT-012 (Enemy Design) and connecting them to the Loot System.
1. Architecture & Data Flow
	•	Template Pattern: Use EnemyTemplate records to define static base stats (HP, Attributes) for species (e.g., "Rusted Servitor").
	•	Factory Pattern: EnemyFactory inflates templates into live Enemy entities, applying variance (HP +/- 5%) and equipment.
2. Key Code Changes File: RuneAndRust.Core/Models/Combat/EnemyTemplate.cs
public record EnemyTemplate(
    string Id,
    string Name,
    string Description,
    ArchetypeType Archetype, // Tank, DPS, Glass Cannon, etc.
    int BaseHp,
    int BaseStamina,
    Dictionary<Attribute, int> Attributes,
    List<string> AbilityIds,
    string LootTableId,
    int ThreatTier // 1=Minion, 2=Standard, 3=Elite, 4=Boss
);
File: RuneAndRust.Engine/Services/EnemyFactory.cs
public Enemy CreateFromTemplate(string templateId, int partyLevel)
{
    var template = _templateRepository.GetById(templateId);

    // Scale stats based on TDR/Level (SPEC-COMBAT-017)
    var scaler = _scalingService.GetMultiplier(template.ThreatTier, partyLevel);

    return new Enemy
    {
        Id = Guid.NewGuid(),
        Name = template.Name,
        MaxHp = (int)(template.BaseHp * scaler.HpMult),
        CurrentHp = (int)(template.BaseHp * scaler.HpMult),
        Attributes = template.Attributes, // Needs deep copy in real impl
        // ...
    };
}
3. Deliverable Checklist (v0.2.2a)
	•	[ ] Core: Define EnemyTemplate and EnemyArchetype enum.
	•	[ ] Engine: Implement EnemyFactory with basic TDR scaling logic.
	•	[ ] Data: Seed initial Bestiary (Rusted Servitor, Ash-Vargr, Corrupted Engineer).
	•	[ ] Tests: EnemyFactory_ScalesStats_Correctly (Level 1 vs Level 5).
	•	Tests: EnemyFactory_AssignsLootTable_Correctly.

v0.2.2b: The Mind (AI Behaviors)
Goal: Implement the EnemyAIService using the probability-based decision trees defined in SPEC-AI-001.
1. Logic Decision Trees (AI Patterns)
	•	Aggressive AI (Ash-Vargr):
	◦	Health < 20%? -> Flee (if not Cornered).
	◦	Target Adjacent? -> Attack (80%) or Power Attack (20%).
	◦	Target Far? -> Move towards lowest HP target.
	•	Defensive AI (Shield-Bearer):
	◦	Ally < 50% HP? -> Guard ally.
	◦	Self HP < 50%? -> Defensive Stance.
	◦	Else: -> Attack closest target.
2. Key Code Changes File: RuneAndRust.Engine/Services/EnemyAIService.cs
public CombatAction DetermineAction(Combatant self, CombatState state)
{
    // 1. Identify Pattern
    var pattern = self.EnemySource?.AIProfile ?? AIProfile.Basic;

    // 2. Filter Valid Targets
    var targets = state.TurnOrder.Where(c => c.IsPlayer).ToList();

    // 3. Execute Pattern Logic
    return pattern switch
    {
        AIProfile.Aggressive => ExecuteAggressive(self, targets),
        AIProfile.Defensive => ExecuteDefensive(self, targets),
        _ => ExecuteBasic(self, targets)
    };
}

private CombatAction ExecuteAggressive(Combatant self, List<Combatant> targets)
{
    var weakest = targets.OrderBy(t => t.CurrentHp).First();
    if (_dice.Roll(100).Total < 80)
        return new CombatAction(ActionType.Attack, weakest.Id);

    return new CombatAction(ActionType.Ability, weakest.Id, "HeavyStrike");
}
3. Deliverable Checklist (v0.2.2b)
	•	[ ] Core: Define AIProfile enum (Aggressive, Defensive, Tactical, Swarm).
	•	[ ] Engine: Implement EnemyAIService with pattern switching.
	•	[ ] Engine: Integrate AI step into CombatService.NextTurn.
	•	[ ] Logging: Add "[AI] {Name} decided to {Action} target {Target}".
	•	[ ] Tests: AI_Aggressive_PrioritizesWeakestTarget.
	•	[ ] Tests: AI_Defensive_TurtlesAtLowHealth.

v0.2.2c: The Elite (Traits & Affixes)
Goal: Implement the CreatureTraitService (SPEC-COMBAT-015) to generate Elite and Champion enemies with modifiers like "Explosive" or "Armored."
1. Implementation Workflow
	1	Trait Registry: Create a dictionary of traits (e.g., Explosive = OnDeath deal 2d6 AoE).
	2	Generation: Update EnemyFactory to roll for Elite status (10% chance) and assign 1-3 traits.
	3	Hooks: Update CombatService to check for Trait triggers (OnHit, OnDeath, OnTurnStart).
2. Key Code Changes File: RuneAndRust.Engine/Services/CreatureTraitService.cs
public void ApplyTraitEffects(GameEvent gameEvent)
{
    if (gameEvent is CombatDeathEvent deathEvent)
    {
        var traits = deathEvent.Victim.Traits;
        if (traits.Contains(CreatureTrait.Explosive))
        {
            // Logic to deal AoE damage
            _logger.LogInformation("{Name} explodes on death!", deathEvent.Victim.Name);
            _combatService.ApplyAreaDamage(deathEvent.Victim.Id, 10);
        }
    }
}
3. Deliverable Checklist (v0.2.2c)
	•	[ ] Core: Define CreatureTrait enum.
	•	[ ] Engine: Implement CreatureTraitService.
	•	[ ] Engine: Hook TraitService into Combat Event Bus (OnDamage, OnDeath).
	•	[ ] Visuals: Update CombatScreenRenderer to show star icons [*] or color codes for Elites.
	•	[ ] Tests: Trait_Explosive_DamagesAdjacentOnDeath.

Testing & Validation Strategy
Unit Testing Suite:
	•	EnemyFactoryTests.cs: Verify stats fall within variance ranges.
	•	EnemyAITests.cs: Mock RNG to force specific AI choices and verify correct Action output.
	•	TraitTests.cs: Verify traits modify stats (e.g., Armored adds +2 Soak) correctly.
Logging Matrix:
System
Event
Level
Message Template
AI
Decision
Debug
"{Enemy} (HP:{HP}%) selected behavior {Behavior} -> {Action}"
Factory
Spawn
Info
"Spawned {Rarity} {Name} (Traits: {Traits})"
Traits
Trigger
Info
"{Name}'s {Trait} triggered! Effect: {Effect}"

Draft Changelog (v0.2.2)
# Changelog: v0.2.2 - The Adversary
**Release Date:** 2025-XX-XX

## Summary
The "punching bags" fight back. This update introduces the Enemy AI system, defined Bestiary templates, and the Elite enemy system.

## Features
- **Enemy AI Profiles:** Enemies now utilize Aggressive, Defensive, or Tactical logic based on their archetype.
    - *Aggressive:* Focuses weak targets, rarely flees.
    - *Defensive:* Guards allies, turtles when wounded.
- **The Bestiary:** Implemented templates for 5 core enemies (Servitor, Vargr, Engineer, Drone, Skeleton).
- **Elite System:** Enemies have a chance to spawn as **Elites** or **Champions** with randomized traits.
    - *Explosive:* Detonates on death.
    - *Armored:* High base Soak.
    - *Regenerating:* Recovers HP per turn.
- **Combat Logic:** Enemies now consume Stamina and utilize distinct attack types.

## Technical
- Implemented `EnemyFactory` for procedural stat generation.
- Added `EnemyAIService` decision trees.
- Integrated `CreatureTraitService` into the Combat Event loop.
This plan prepares the engine for the final combat milestone components: Bosses (v0.6.2) and Stealth (v0.6.3), which will be addressed in future updates.
Ready to begin v0.2.2a implementation?
