{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "legacy-class-mappings.schema.json",
  "title": "Legacy Class Mappings",
  "description": "Schema for legacy character class to Aethelgard archetype migration mappings.",
  "type": "object",
  "required": ["version", "mappings"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "JSON Schema reference."
    },
    "version": {
      "type": "string",
      "description": "Version of the configuration format.",
      "pattern": "^\\d+\\.\\d+\\.\\d+[a-z]?$"
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of this configuration file."
    },
    "mappings": {
      "type": "array",
      "description": "Array of legacy class to archetype mapping definitions.",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/legacy-class-mapping"
      }
    }
  },
  "additionalProperties": false,
  "$defs": {
    "legacy-class-mapping": {
      "type": "object",
      "description": "A single legacy class to archetype migration mapping.",
      "required": [
        "legacy-class-id",
        "legacy-class-display-name",
        "target-archetype",
        "suggested-specializations",
        "grants-free-specialization",
        "migration-description"
      ],
      "properties": {
        "legacy-class-id": {
          "type": "string",
          "description": "Kebab-case identifier for the legacy class.",
          "enum": ["rogue", "fighter", "mage", "healer", "scholar", "crafter"]
        },
        "legacy-class-display-name": {
          "type": "string",
          "description": "Human-readable display name for the legacy class.",
          "minLength": 1
        },
        "target-archetype": {
          "type": "string",
          "description": "The Aethelgard archetype this legacy class maps to.",
          "enum": ["warrior", "skirmisher", "mystic", "adept"]
        },
        "suggested-specializations": {
          "type": "array",
          "description": "Kebab-case IDs of suggested specializations for this migration path.",
          "minItems": 1,
          "items": {
            "type": "string",
            "description": "Specialization ID in kebab-case format.",
            "minLength": 1
          }
        },
        "grants-free-specialization": {
          "type": "boolean",
          "description": "Whether this migration grants the first specialization at 0 PP cost."
        },
        "migration-description": {
          "type": "string",
          "description": "Narrative text explaining the migration path to the player.",
          "minLength": 10
        }
      },
      "additionalProperties": false
    }
  }
}
