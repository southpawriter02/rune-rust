Here is the comprehensive implementation plan for v0.3.3a: The Ground (Dynamic Hazards).
This version transforms static rooms into interactive battlefields. It implements the Three-State Lifecycle (Dormant, Triggered, Cooldown) for environmental objects and integrates them with the Combat Loop.

v0.3.3a: The Ground (Dynamic Hazards)
Goal: Implement the HazardService, define the DynamicHazard entity, and wire the trigger logic (Movement, Damage, Interaction) into the engine.

1. Architecture & Data Flow
The HazardService acts as an event listener within the GameLoop and CombatService. It intercepts specific actions (Movement, Damage Application) to check for hazard triggers before or after the action resolves.
Workflow: The Hazard Trigger Cycle
	1	Event: A character attempts to move into a tile OR takes damage in a specific tile.
	2	Lookup: HazardService queries the Room for hazards at that Coordinate.
	3	State Validation:
	◦	Is the hazard [Dormant]? -> Proceed.
	◦	Is it [Triggered], [Cooldown], or [Destroyed]? -> Abort.
	4	Condition Check: Does the event match the TriggerType (e.g., PhysicalDamage, WeightPressure)?.
	5	Execution:
	◦	Call AbilityService to parse the hazard's EffectScript (e.g., DAMAGE:Fire:2d6).
	◦	Update Hazard State (e.g., set CooldownRemaining = 2).
	◦	Chain Reaction: Check if the effect triggers adjacent hazards.
	6	Feedback: Emit HazardTriggeredEvent for UI/Log.

2. Logic Decision Trees
A. Trigger Evaluation Logic
Input: EventType, TargetTile, SourceAmount (optional)
	1	Scan: Find hazards at TargetTile.
	2	Loop: For each hazard:
	◦	State Check: If State != Dormant, Skip.
	◦	Type Check:
	▪	If Trigger == Movement: Activate.
	▪	If Trigger == Damage: Is DamageAmount > DamageThreshold?
	▪	Yes: Activate.
	▪	No: Log "The hazard shudders but holds."
	3	Activate:
	◦	Execute EffectScript against entities in TargetArea.
	◦	If OneTimeUse -> Set State Destroyed.
	◦	Else -> Set State Cooldown.
B. Chain Reaction Logic
Input: Hazard A activates
	1	Identify Area: Get all tiles affected by Hazard A's effect (e.g., Blast Radius).
	2	Scan Neighbors: Find Dormant hazards in those tiles.
	3	Evaluate: Does Hazard A's DamageType trigger Hazard B?
	◦	Example: Fire explosion hits a "Gas Canister" (Trigger: Fire).
	4	Execute: Trigger Hazard B immediately (Recursion limited to depth 5 to prevent infinite loops).

3. Code Implementation
A. Core Layer (Models)
File: RuneAndRust.Core/Entities/DynamicHazard.cs
public class DynamicHazard : InteractableObject
{
    public HazardType Type { get; set; } // Mechanical, Environmental, etc.
    public HazardState State { get; set; } = HazardState.Dormant;
    public int CooldownRemaining { get; set; }
    public int MaxCooldown { get; set; } // e.g., 2 rounds for Steam Vent
    public bool OneTimeUse { get; set; } // True for Spore Pods

    // Trigger Logic
    public TriggerType Trigger { get; set; } // Movement, Damage, Interaction
    public DamageType? TriggerDamageType { get; set; } // e.g., Fire required to ignite oil
    public int DamageThreshold { get; set; } // Minimum damage to trigger

    // Execution
    public string EffectScript { get; set; } // "DAMAGE:Fire:2d6;STATUS:Blind:1"
    public int Radius { get; set; } = 0; // 0 = Single Tile
}

public enum HazardState { Dormant, Triggered, Cooldown, Destroyed }
public enum TriggerType { Movement, DamageTaken, ManualInteraction, TurnStart }
B. Engine Layer (Service)
File: RuneAndRust.Engine/Services/HazardService.cs
public async Task ProcessDamageTriggerAsync(Room room, Coordinate location, DamageType type, int amount)
{
    // 1. Identify Hazards
    var hazards = room.GetHazardsAt(location)
                      .Where(h => h.State == HazardState.Dormant)
                      .Where(h => h.Trigger == TriggerType.DamageTaken);

    foreach (var hazard in hazards)
    {
        // 2. Validate Condition
        if (hazard.TriggerDamageType.HasValue && hazard.TriggerDamageType != type) continue;
        if (amount < hazard.DamageThreshold) continue;

        // 3. Execute
        await ActivateHazardAsync(hazard, room);
    }
}

private async Task ActivateHazardAsync(DynamicHazard hazard, Room room)
{
    // State Transition
    hazard.State = HazardState.Triggered;

    // Execute Script via existing AbilityService infrastructure
    var targets = _gridService.GetEntitiesInRadius(room, hazard.Position, hazard.Radius);
    foreach(var target in targets)
    {
        await _abilityService.ExecuteScriptAsync(hazard.EffectScript, null, target);
    }

    // Cooldown Management
    if (hazard.OneTimeUse)
    {
        hazard.State = HazardState.Destroyed;
        _logger.LogDebug("[{Hazard}] destroyed after trigger", hazard.Name);
    }
    else
    {
        hazard.State = HazardState.Cooldown;
        hazard.CooldownRemaining = hazard.MaxCooldown;
    }

    // Chain Reaction Check (Simplified)
    await CheckChainReactionAsync(hazard, room);
}

4. Logging Requirements
Reference: logging-matrix.md and
System
Event
Level
Message Template
Properties
Hazard
Triggered
Info
"[{Hazard}] triggered by {Trigger} at {Position}"
Hazard, Trigger, Position
Hazard
Effect
Debug
"[{Hazard}] dealt {Damage} to {Targets}"
Hazard, Damage, Targets
Hazard
Chain
Info
"Chain reaction: [{Source}] triggered [{Target}]"
Source, Target
Hazard
Destroyed
Debug
"[{Hazard}] destroyed after one-time trigger"
Hazard
Hazard
Cooldown
Trace
"[{Hazard}] cooldown decremented: {Remaining}"
Hazard, Remaining

5. Testing Strategy
Unit Tests (HazardServiceTests.cs)
	•	Trigger_Validation:
	◦	ProcessDamage_FireHitSteamVent_Triggers: Assert State becomes Cooldown.
	◦	ProcessDamage_PhysicalHitOil_DoesNotTrigger: Verify specific damage type constraint.
	◦	ProcessMovement_StepOnPlate_Triggers: Assert EffectScript executes.
	•	Cooldowns:
	◦	ProcessTurnEnd_DecrementsCooldown: 2 -> 1.
	◦	ProcessTurnEnd_ResetsToDormant: 0 -> State Dormant.
	•	OneTimeUse:
	◦	Trigger_SporePod_SetsDestroyed: Verify it acts only once.
Integration Tests
	•	Chain_Reaction:
	◦	Place ExplosiveBarrel adjacent to SteamVent. Trigger Barrel. Assert Vent triggers (due to Fire damage).
	•	Combat_Integration:
	◦	Push Enemy into SpikeTrap. Verify Enemy takes damage immediately.

6. Deliverable Checklist (v0.3.3a)
	•	Core:
	◦	[ ] Define DynamicHazard entity extending InteractableObject.
	◦	[ ] Define HazardState and TriggerType enums.
	◦	[ ] Add List<DynamicHazard> to Room entity.
	•	Engine:
	◦	[ ] Implement HazardService with ActivateHazardAsync.
	◦	[ ] Update CombatService to call HazardService.ProcessDamageTrigger on every attack.
	◦	[ ] Update NavigationService to call HazardService.ProcessMovementTrigger on every step.
	•	Data:
	◦	[ ] Seed Steam Vent: Trigger=Damage/Phys|Fire, Effect=2d6 Fire.
	◦	[ ] Seed Pressure Plate: Trigger=Movement, Effect=Arrow Trap (1d8 Phys).
	◦	[ ] Seed Volatile Spore Pod: Trigger=Damage, Effect=Poison Cloud.
	•	UI:
	◦	[ ] Update TUI to render Hazards (Grey=Dormant, Red=Triggered).
	◦	[ ] Add "Hazard" column to look command output.

7. Draft Changelog (v0.3.3a)
# Changelog: v0.3.3a - The Ground (Dynamic Hazards)
**Release Date:** 2025-XX-XX

## Summary
The battlefield is no longer static. This update introduces **Dynamic Hazards**—interactive objects that react to combat. Players and enemies must now navigate around Steam Vents, Pressure Plates, and Spore Pods, or risk triggering them for tactical advantage.

## Features
- **Dynamic Hazard System:**
    - **Interactive Objects:** Hazards exist in the world and react to Movement, Damage, or Interaction.
    - **Lifecycle:** Hazards cycle between **[Dormant]**, **[Triggered]**, and **[Cooldown]** states.
    - **Chain Reactions:** Explosions can trigger nearby hazards, creating cascades of destruction.
- **New Hazards:**
    - **Steam Vent:** Ruptures when hit, dealing Fire damage and obscuring vision.
    - **Pressure Plate:** Mechanical trigger that activates traps when stepped on.
    - **Volatile Spore Pod:** Explodes into a poison cloud if damaged.
- **Combat Integration:**
    - Pushing enemies into hazards is now a valid tactic.
    - Area of Effect (AoE) attacks can accidentally trigger environmental dangers.

## Technical
- Implemented `HazardService` and `DynamicHazard` entity.
- Integrated `EffectScript` parser for hazard consequences.
- Added `[Hazard]` layer to Room generation.
