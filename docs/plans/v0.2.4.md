Here is the comprehensive implementation plan for v0.2.4: The Adversary's Arsenal.
This version bridges the gap between the Ability System (v0.2.3) and the Enemy AI (v0.2.2). It grants enemies access to Active Abilities, allowing them to use the same EffectScript engine as players, while introducing AI logic to select those abilities intelligently.
To manage complexity, we will split this into three sub-versions:
	•	v0.2.4a: The Loadout (Data Integration) – Database schemas, seeding enemy abilities, and hydrating combatants.
	•	v0.2.4b: The Tactician (Selection Logic) – AI evaluation scoring, resource management for enemies, and cooldown handling.
	•	v0.2.4c: The Omen (Telegraphs & Chants) – Multi-turn attacks, "Charging" states, and UI integration for telegraphed moves.

v0.2.4: The Adversary's Arsenal
v0.2.4a: The Loadout (Data Integration)
Goal: Establish the data relationship between EnemyTemplate and ActiveAbility. Ensure enemies spawn with their specific move sets hydrated and ready for use.
1. Architecture & Data Flow
	•	Data Structure: A Many-to-Many relationship between EnemyTemplates and ActiveAbilities.
	•	Instantiation:
	1	EncounterService requests an Enemy.
	2	EnemyFactory loads the Template.
	3	New Step: Factory queries EnemyAbilityRepository for linked Ability IDs.
	4	Combatant.FromEnemy initializes the Abilities list (previously empty for enemies).
2. Logic Decision Trees
	•	Hydration Logic:
	1	Load Enemy Template (e.g., "Rusted Draugr").
	2	Fetch Linked Ability IDs (e.g., "Rusty Cleave", "Shield Bash").
	3	Fetch Ability Data from ActiveAbilityRepository.
	4	Assign to Combatant.Abilities.
3. Code Implementation File: RuneAndRust.Core/Models/Combat/EnemyTemplate.cs
public record EnemyTemplate(
    // ... existing props
    List<Guid> AbilityIds // New: List of abilities this enemy knows
);
File: RuneAndRust.Persistence/Data/EnemySeeder.cs
// Seeding specific moves
var rustyCleave = new ActiveAbility {
    Name = "Rusty Cleave",
    EffectScript = "DAMAGE:Physical:1d8+2;STATUS:Tetanus:3"
};
4. Deliverable Checklist (v0.2.4a)
	•	[ ] Persistence: Create EnemyAbilities join table in DbContext.
	•	[ ] Core: Update EnemyTemplate to include AbilityIds.
	•	[ ] Engine: Update EnemyFactory to hydrate abilities during creation.
	•	[ ] Data: Seed 5-10 enemy-specific abilities (e.g., Screech, Piston Punch).

v0.2.4b: The Tactician (Selection Logic)
Goal: Update EnemyAIService to evaluate and select abilities instead of just using Basic Attack.
1. Architecture & Data Flow
	•	Evaluation: The AI scores every possible action (Basic Attack, Defend, Ability A, Ability B) based on the current state.
	•	Criteria:
	◦	Cost: Can I afford the Stamina/Aether?
	◦	Cooldown: Is it ready?
	◦	Utility: Is the target low HP? Do I need to heal?
2. Logic Decision Trees
	•	AI Decision Loop:
	1	Identify Options: Gather Basic Attack + Combatant.Abilities.
	2	Filter: Remove options on Cooldown or Insufficient Resources.
	3	Score:
	▪	Damage Ability: Base Score 50. +20 if Target HP < 30% (Execute range).
	▪	Heal Ability: Base Score 0. +80 if Self HP < 50%.
	▪	Debuff Ability: Base Score 40. -100 if Target already has Status.
	4	Select: Pick highest score (with small RNG variance).
3. Code Implementation File: RuneAndRust.Engine/Services/EnemyAIService.cs
public CombatAction DetermineAction(Combatant enemy, CombatState state)
{
    var validAbilities = enemy.Abilities
        .Where(a => _abilityService.CanUse(enemy, a))
        .ToList();

    // Simple heuristic: If a special move is ready and affordable, use it 70% of the time
    if (validAbilities.Any() && _random.NextDouble() > 0.3)
    {
        var selected = validAbilities.First(); // Logic to pick best one goes here
        return new CombatAction(ActionType.UseAbility, enemy.Id, TargetId: player.Id, AbilityId: selected.Id);
    }

    return new CombatAction(ActionType.Attack, enemy.Id, TargetId: player.Id);
}
4. Deliverable Checklist (v0.2.4b)
	•	[ ] Engine: Update EnemyAIService to check AbilityService.CanUse().
	•	[ ] Engine: Update CombatService to execute ActionType.UseAbility for enemies.
	•	[ ] Engine: Implement resource deduction (Stamina/Aether) for enemies.

v0.2.4c: The Omen (Telegraphs & Chants)
Goal: Implement multi-turn "Chant" abilities for enemies, allowing them to telegraph powerful attacks (e.g., "The Golem begins to wind up...") and giving players a turn to react/interrupt.
1. Architecture & Data Flow
	•	Chant State: Combatant tracks ActiveChant (AbilityID, TurnsRemaining).
	•	UI Integration: Connects with v0.3.6c (Intent System) to display warning icons.
	•	Interruption: Dealing damage > Threshold to a chanting enemy cancels the effect.
2. Logic Decision Trees
	•	Chant Execution:
	1	Turn 1: Enemy selects "Power Slam" (Chant: 1 Turn).
	2	State: Set Combatant.ChantState. Log: "Enemy begins charging Power Slam!"
	3	Turn 2 (Start): Decrement Chant Timer.
	4	Resolution: Timer == 0? Execute Ability Effect immediately. Reset State.
	•	Interruption Logic:
	1	Player attacks Enemy.
	2	Is Enemy Chanting?
	3	Is Damage > InterruptionThreshold?
	▪	Yes: Clear ChantState. Apply [Stunned]. Log: "Chant Interrupted!"
3. Code Implementation File: RuneAndRust.Core/Models/Combat/Combatant.cs
public ActiveAbility? CurrentChant { get; set; }
public int ChantTurnsRemaining { get; set; }
4. Deliverable Checklist (v0.2.4c)
	•	[ ] Core: Update ActiveAbility entity with ChantDuration and InterruptThreshold.
	•	[ ] Engine: Update CombatService to handle Chant lifecycles (Start, Tick, Execute).
	•	[ ] UI: Update CombatGridRenderer to show "⚠ CHARGING" status.

5. Logging & Metrics
System
Event
Level
Message Template
Properties
AI
Ability Select
Info
"[AI] {Name} selected ability {Ability} (Score: {Score})"
Name, Ability, Score
Combat
Chant Start
Warn
"{Name} begins chanting {Ability}! ({Turns} turns)"
Name, Ability, Turns
Combat
Interrupted
Info
"{Name}'s chant was interrupted by damage!"
Name

6. Testing Requirements
	•	Unit Tests (EnemyAIServiceTests.cs):
	◦	DetermineAction_PrefersHeal_WhenLowHP: Set Enemy HP to 10%. Verify Heal ability selected.
	◦	DetermineAction_RespectsCooldowns: Put Ability A on cooldown. Verify Ability B or Attack selected.
	•	Integration Tests (CombatChantTests.cs):
	◦	Chant_Executes_AfterDelay: Start Chant -> Next Turn -> Effect Applied.
	◦	Chant_Interrupted_ByDamage: Start Chant -> Deal 20 Dmg -> Verify Chant cleared and Effect NOT applied.

7. Draft Changelog (v0.2.4)
# Changelog: v0.2.4 - The Adversary's Arsenal
**Release Date:** 2025-XX-XX

## Summary
"The Adversary's Arsenal" gives enemies access to the full range of combat mechanics previously reserved for players. Enemies now spawn with specific ability loadouts defined by their template and use AI logic to select the best move for the situation. This update also introduces "Chants"—powerful, telegraphed attacks that require players to defend or interrupt, adding a new tactical layer to boss fights and elite encounters.

## Features
- **Enemy Abilities:**
    - Enemies now use Stamina/Aether to cast abilities.
    - Added unique movesets for Draugr (Sunder Armor), Servitors (Repair), and Vargr (Howl).
- **Smart AI:**
    - Enemies evaluate HP, Cooldowns, and Status Effects before acting.
    - Healers will prioritize wounded allies; Strikers will target vulnerable players.
- **Chant System:**
    - Powerful attacks now require 1+ turns to charge.
    - Visual indicators warn players of incoming heavy hits.
    - Dealing sufficient damage interrupts the chant, stunning the enemy.

## Technical
- Extended `EnemyTemplate` to include Ability IDs.
- Refactored `EnemyAIService` to score actions based on utility.
- Added Chant state management to `CombatService`.
