Here is the comprehensive implementation plan for v0.3.3c: The Ecosystem (Integration).
This version synthesizes the mechanics from v0.3.3a (Hazards) and v0.3.3b (Conditions) into the procedural generation pipeline and the enemy AI, ensuring the world feels cohesive and inhabitants react intelligently to their environment.

v0.3.3c: The Ecosystem (Integration)
Goal: Update DungeonGenerator to seed biome-appropriate hazards/conditions and upgrade EnemyAIService to factor environmental dangers into decision-making.

1. Architecture & Data Flow
Workflow A: Thematic Generation
	1	Trigger: DungeonGenerator creates a Room.
	2	Context: Generator checks Room.BiomeType (e.g., Industrial) and Room.DangerLevel.
	3	Selection:
	◦	Condition: Roll against DangerLevel to apply a room-wide AmbientCondition (e.g., ToxicAtmosphere).
	◦	Hazards: Consult BiomeHazardTable. Select hazards matching the Biome (e.g., SteamVent for Industrial).
	4	Placement: Add entities to Room.Hazards and set Room.ConditionId.
Workflow B: AI Hazard Avoidance
	1	Trigger: Enemy turn starts. EnemyAIService evaluates movement options.
	2	Scan: Identify adjacent tiles.
	3	Analysis: Check tiles for DynamicHazard.
	◦	If Hazard is Active and Harmful: Check Enemy Traits (e.g., is it IronHeart vs Poison?).
	4	Scoring: Apply negative weight to hazardous tiles.
	5	Action: Select move with best score.

2. Logic Decision Trees
A. Biome Population Logic
Input: BiomeType, DangerLevel
	1	Ambient Condition Roll:
	◦	Chance = DangerLevel * 10%.
	◦	If Hit: Select from ConditionRegistry where Biome matches.
	2	Hazard Density Roll:
	◦	Count = Random(Min, Max) scaled by DangerLevel.
	3	Hazard Selection:
	◦	Filter HazardRegistry by BiomeType.
	◦	Instantiate DynamicHazard.
	◦	Assign random X/Y coordinates within Room bounds.
B. AI Move Evaluation
Input: Enemy, TargetTile
	1	Is there a Hazard? -> No: Score = 0.
	2	Is Hazard Dangerous? (State != Destroyed/Cooldown) -> No: Score = 0.
	3	Is Enemy Immune?
	◦	Hazard is Poison AND Enemy has Mechanical tag? -> Yes: Score = 0.
	◦	Hazard is Fire AND Enemy has Thermal trait? -> Yes: Score = 0.
	4	Result: Apply -50 Score (Strong deterrent, but overrides if it's the only path to a kill).

3. Code Implementation
A. Engine Layer (Generation)
File: RuneAndRust.Engine/Services/DungeonGenerator.cs (Extension)
private async Task PopulateEnvironmentAsync(Room room)
{
    // 1. Ambient Conditions
    int conditionChance = (int)room.DangerLevel * 10;
    if (_dice.Roll1d100() <= conditionChance)
    {
        var condition = _conditionRegistry.GetRandomForBiome(room.BiomeType);
        room.ConditionId = condition.Id;
    }

    // 2. Dynamic Hazards
    int hazardCount = _dice.Roll(1, (int)room.DangerLevel + 1); // 1 to 4 hazards
    for(int i = 0; i < hazardCount; i++)
    {
        var template = _hazardRegistry.GetRandomForBiome(room.BiomeType);
        var hazard = _objectFactory.CreateHazard(template);

        // Random placement (simplified)
        hazard.Position = new Coordinate(
            _dice.Roll(0, room.Width),
            _dice.Roll(0, room.Height),
            0
        );

        room.Hazards.Add(hazard);
    }
}
B. Engine Layer (AI Update)
File: RuneAndRust.Engine/Services/EnemyAIService.cs (Update)
public int EvaluateTileSafety(Combatant enemy, Coordinate tile, Room room)
{
    int safetyScore = 0;

    // Check for hazards at this location
    var hazard = room.Hazards.FirstOrDefault(h => h.Position == tile && h.State == HazardState.Dormant);

    if (hazard != null)
    {
        // Check for specific immunities via Traits (SPEC-COMBAT-015)
        if (hazard.RequiredDamageType == DamageType.Poison && enemy.Tags.Contains("Mechanical"))
        {
            return 0; // Safe for robots
        }

        // Check for Mindless trait (Zombies walk into fire)
        if (enemy.ActiveTraits.Any(t => t.Name == "Mindless"))
        {
            return 0; // Ignores danger
        }

        safetyScore -= 50; // Heavy penalty
    }

    return safetyScore;
}

4. Logging Requirements
System
Event
Level
Message Template
Properties
Generator
Condition Set
Info
"Set Room {Room} condition to {Condition}."
Room, Condition
Generator
Hazard Spawn
Debug
"Spawned {Hazard} at {Pos} in {Room}."
Hazard, Pos, Room
AI
Hazard Avoid
Trace
"{Enemy} avoided {Tile} due to {Hazard} (Score -50)."
Enemy, Tile, Hazard
AI
Immunity
Trace
"{Enemy} ignored {Hazard} due to {Tag}."
Enemy, Hazard, Tag

5. Testing Requirements
Unit Tests (DungeonGeneratorTests.cs)
	•	Biome_Consistency:
	◦	Generate_IndustrialBiome_SpawnsSteamVents: Mock dice, verify Hazard type.
	◦	Generate_OrganicBiome_SpawnsSporePods: Verify correct registry lookup.
	•	Density:
	◦	Generate_SafeRoom_SpawnsZeroHazards: Verify DangerLevel.Safe logic.
	◦	Generate_LethalRoom_SpawnsMultipleHazards: Verify loop count.
Unit Tests (EnemyAIServiceTests.cs)
	•	Avoidance:
	◦	EvaluateMove_TileWithActiveTrap_ReturnsNegativeScore.
	◦	EvaluateMove_TileWithDestroyedTrap_ReturnsNeutralScore.
	•	Intelligence:
	◦	EvaluateMove_MindlessEnemy_IgnoresTrapScore.
	◦	EvaluateMove_ImmuneEnemy_IgnoresSpecificHazard.

6. Deliverable Checklist (v0.3.3c)
	•	Data:
	◦	[ ] Update HazardRegistry with Biome tags (e.g., SteamVent -> Industrial).
	◦	[ ] Update ConditionRegistry with Biome tags (e.g., Toxic -> Industrial/Helheim).
	•	Engine:
	◦	[ ] Update DungeonGenerator to call PopulateEnvironmentAsync.
	◦	[ ] Update EnemyAIService to include EvaluateTileSafety.
	◦	[ ] Inject ICreatureTraitService into AI to check for "Mindless" or Immunities.
	•	Persistence:
	◦	[ ] Ensure Room.Hazards and Room.ConditionId are saved/loaded correctly (JsonB verification).

7. Draft Changelog (v0.3.3)
# Changelog: v0.3.3 - Dynamic Environment
**Release Date:** 2025-XX-XX

## Summary
Milestone 4 is complete. The dungeon environment is no longer static scenery but an active participant in exploration and combat. This update integrates the Hazard and Condition systems into procedural generation and enemy artificial intelligence.

## Features
- **Thematic Generation:**
    - **Biome Alignment:** Rooms now spawn hazards and conditions matching their theme. Industrial zones feature Steam Vents and Toxic Air; Organic zones feature Spore Pods.
    - **Danger Scaling:** 'Lethal' rooms now contain significantly denser hazard fields.
- **Intelligent AI:**
    - **Self-Preservation:** Enemies (mostly) avoid walking into active traps or fire vents.
    - **Mindless Enemies:** Undying and certain beasts ignore hazards, relentlessly pursuing the player even through fire.
    - **Immunity Logic:** Mechanical enemies will now wade through Poison clouds to reach you, exploiting their natural immunities.

## Technical
- Integrated `HazardRegistry` into `DungeonGenerator`.
- Updated `EnemyAIService` to query `HazardService` during pathfinding.
- Completed "The Ecosystem" integration layer.
