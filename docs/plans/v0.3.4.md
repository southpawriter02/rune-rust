Here is the comprehensive implementation plan for v0.3.4: The Gateway (Menu & Creation).
This milestone replaces the debug-style startup with an immersive, "glitched" terminal interface using Spectre.Console. It establishes the tone of Aethelgard immediately upon launch.
To manage the UI complexity, this version is split into three sub-versions:
	•	v0.3.4a: The Facade (Visuals & Menu) – ASCII art engine, glitch effects, and main menu navigation.
	•	v0.3.4b: The Forge (Creation Wizard) – Split-screen character creator with live stat previews.
	•	v0.3.4c: The Prologue (Narrative) – Typewriter intro system and seamless game loop transition.

v0.3.4: The Gateway (Menu & Creation)
v0.3.4a: The Facade (Visuals & Menu)
Goal: Implement the TitleScreenService and AsciiArtRepository to render an animated, atmospheric entry point.
1. Architecture & Data Flow
	•	Asset Loading: On startup, load ASCII assets (Logo, Borders).
	•	Animation Loop: Use AnsiConsole.Live to render the Title Logo with a "Glitch" effect (random character replacement representing the Runic Blight).
	•	Input Handling: Intercept keys to stop animation and render the Interactive Menu.
2. Logic Decision Trees
	•	Glitch Effect Logic:
	1	Render Frame.
	2	Is RNG < GlitchChance?
	▪	Yes: Select random characters in logo string. Replace with Rune symbols (ᚠ, ᚢ, ᚦ). Change color to Red/Purple.
	▪	No: Render standard Gold/Rust colors.
	3	Wait 100ms. Repeat until Input.
	•	Menu Routing:
	◦	New Game: -> Transition to CreationWizard.
	◦	Load Game: -> Transition to LoadScreen (v0.0.4 logic).
	◦	Options: -> Open Settings overlay.
	◦	Quit: -> Terminate.
3. Code Implementation File: RuneAndRust.Terminal/Services/TitleScreenService.cs
public async Task ShowTitleAsync()
{
    var font = FigletFont.Load("assets/runic.flf");
    var logo = new FigletText(font, "Rune & Rust").Color(Color.Gold1);

    await AnsiConsole.Live(logo)
        .StartAsync(async ctx =>
        {
            while (!Console.KeyAvailable)
            {
                // Glitch Logic
                if (_random.NextDouble() < 0.05)
                {
                    logo.Color(Color.Red1);
                    // (Additional logic to scramble text would go here)
                }
                else
                {
                    logo.Color(Color.Gold1);
                }

                ctx.Refresh();
                await Task.Delay(50);
            }
        });

    Console.ReadKey(true); // Consume keypress
    return ShowMainMenu();
}
4. Deliverable Checklist (v0.3.4a)
	•	[ ] Assets: Create/Import ASCII fonts and Logo text.
	•	[ ] Terminal: Implement TitleScreenService.
	•	[ ] Logic: Implement "Glitch" animation loop.
	•	[ ] Input: Wire Main Menu selection prompt using SelectionPrompt<T>.

v0.3.4b: The Forge (Creation Wizard)
Goal: Upgrade CharacterCreationController into a split-screen interactive wizard that shows "Live Previews" of stats as the player browses options.
1. Architecture & Data Flow
	•	Layout: Two-column Grid.
	◦	Left: SelectionPrompt (Lineage, Archetype, Background).
	◦	Right: Panel showing detailed stats (HP, Stamina, Attributes) of the currently highlighted option.
	•	State: WizardContext stores temporary selections.
	•	Live Update: When the user moves the selection cursor (but hasn't pressed Enter), update the Right Panel.
2. Logic Decision Trees
	•	Preview Calculation:
	1	Input: Highlighted Archetype (e.g., Warrior).
	2	Fetch: Get base attributes for Warrior.
	3	Modify: Apply bonuses from previously selected Lineage (stored in Context).
	4	Derive: Calculate HP/Stamina/AP using StatCalculationService.
	5	Render: Update Right Panel with "Projected Stats".
	•	Navigation:
	◦	Next: Confirm selection -> Add to Context -> Move to next step.
	◦	Back: Remove last selection -> Return to previous step.
3. Code Implementation File: RuneAndRust.Terminal/UI/CreationWizard.cs
public Character Run()
{
    var context = new WizardContext();

    // Step 1: Lineage
    var layout = new Layout("Root").SplitColumns(new Layout("Menu"), new Layout("Preview"));

    AnsiConsole.Live(layout).Start(ctx => {
        // Pseudo-code: Spectre's SelectionPrompt doesn't natively support
        // "OnHighlight" callbacks easily without a custom loop.
        // Implementation will likely use a custom render loop reading Key.Up/Down
        // to update the "Preview" layout before redrawing.
    });

    // ... Repeat for Archetype ...

    return _factory.Create(context.Name, context.Lineage, context.Archetype);
}
4. Deliverable Checklist (v0.3.4b)
	•	[ ] Core: Define WizardContext to hold transient creation state.
	•	[ ] Terminal: Create LiveStatPreviewPanel class.
	•	[ ] Engine: Update CharacterFactory to accept a context object if needed.
	•	[ ] UI: Implement "Back" functionality in the wizard.

v0.3.4c: The Prologue (Narrative)
Goal: Bridge Character Creation and Gameplay with a scrolling, typewriter-style text intro based on the chosen Background.
1. Architecture & Data Flow
	•	Input: Created Character (specifically Background property).
	•	Data: NarrativeRepository containing intro templates (e.g., "The Scavenger's Intro", "The Exile's Intro").
	•	Process:
	1	Clear Screen.
	2	Fetch specific text.
	3	Write char-by-char with varying delays (slower for punctuation).
	4	Allow Space to speed up, Esc to skip.
	5	Transition to GameService.StartAsync.
2. Deliverable Checklist (v0.3.4c)
	•	[ ] Data: Author intros for the 6 Backgrounds (Domain 4 compliant).
	•	[ ] Terminal: Implement TypewriterRenderer.
	•	[ ] Engine: Wire CreationWizard output -> Prologue -> GameLoop.

5. Logging & Metrics
System
Event
Level
Message Template
Properties
Menu
Startup
Info
"Application started. Version {Ver}."
Ver
Wizard
Step
Debug
"Wizard Step {Step}: Highlighted {Option}."
Step, Option
Wizard
Complete
Info
"Character Created: {Name} ({Lineage} {Archetype})."
Name, Lineage, Archetype
Prologue
Skip
Info
"Player skipped intro sequence."
-

6. Testing Requirements
	•	Unit Tests (TitleScreenTests.cs):
	◦	Menu_ReturnsCorrectSelection: Mock input stream -> Verify Start/Quit returns.
	•	Unit Tests (CharacterWizardTests.cs):
	◦	Preview_CalculatesCorrectStats: Highlight Warrior + Human -> Preview shows correct summed stats.
	◦	Back_ResetsSelection: Select Lineage -> Next -> Back -> Lineage is null.
	•	Integration Tests:
	◦	FullFlow_CreationToGame: Menu Start -> Wizard Complete -> Game Loop Active.

7. Draft Changelog (v0.3.4)
# Changelog: v0.3.4 - The Gateway
**Release Date:** 2025-XX-XX

## Summary
The "Gateway" update replaces the basic CLI prompts with a rich, immersive Terminal UI. Players now begin their journey with an animated title screen, a responsive character creation wizard with live stat previews, and atmospheric narrative introductions.

## Features
- **Visual Title Screen:** Animated ASCII art logo with dynamic "glitch" effects representing the Runic Blight.
- **Interactive Character Creator:**
    - **Split-Screen UI:** View descriptions and stat impacts *before* confirming choices.
    - **Live Preview:** See exactly how "Iron-Blooded" + "Warrior" affects your starting HP and MIGHT.
    - **Navigable:** Use Arrow Keys to browse, Backspace to return to previous steps.
- **Narrative Intros:** Unique typewriter-style prologues for each Background, setting the tone for the Age of Echoes.

## Technical
- Implemented `Spectre.Console.Live` for real-time UI updates during creation.
- Added `NarrativeService` for handling flavor text injection.
